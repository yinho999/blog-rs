// Import the styles from the CSS Module
import styles from "./Home.module.css";

const Home = () => {
  // Use the imported styles
  return <div className={styles.home}>This is a home.</div>;
};

export default Home;
